From 3186f7b6e44b86202fadab0abda4291bfa23c1c0 Mon Sep 17 00:00:00 2001
From: Jilay Pandya <jilay.pandya@outlook.com>
Date: Sat, 7 Mar 2026 17:08:13 +0100
Subject: [PATCH 3/5] feat: add random can tx generator

Signed-off-by: Jilay Pandya <jilay.pandya@outlook.com>
---
 subsys/can_uart_replay/CMakeLists.txt         |   1 +
 subsys/can_uart_replay/Kconfig                |   2 +
 .../Kconfig.can_random_traffic                |  46 ++++++++
 .../Kconfig.template.can_tx_schedule          |  15 +++
 subsys/can_uart_replay/can_random_traffic.c   | 100 ++++++++++++++++++
 5 files changed, 164 insertions(+)
 create mode 100644 subsys/can_uart_replay/Kconfig.can_random_traffic
 create mode 100644 subsys/can_uart_replay/Kconfig.template.can_tx_schedule
 create mode 100644 subsys/can_uart_replay/can_random_traffic.c

diff --git a/subsys/can_uart_replay/CMakeLists.txt b/subsys/can_uart_replay/CMakeLists.txt
index bdf6518..28ab3fe 100644
--- a/subsys/can_uart_replay/CMakeLists.txt
+++ b/subsys/can_uart_replay/CMakeLists.txt
@@ -4,3 +4,4 @@
 zephyr_library()
 zephyr_library_sources_ifdef(CONFIG_CAN_UART_REPLAY can_uart_replay.c)
 zephyr_linker_sources_ifdef(CONFIG_CAN_UART_REPLAY ROM_SECTIONS can_uart_replay.ld)
+zephyr_library_sources_ifdef(CONFIG_CAN_RANDOM_TRAFFIC can_random_traffic.c)
diff --git a/subsys/can_uart_replay/Kconfig b/subsys/can_uart_replay/Kconfig
index de3aadd..8be1c01 100644
--- a/subsys/can_uart_replay/Kconfig
+++ b/subsys/can_uart_replay/Kconfig
@@ -77,6 +77,8 @@ config CAN_UART_REPLAY_FILTER_MASK
 	  Power-of-two-aligned ranges are also possible, e.g. 0x700 accepts
 	  IDs 0x100–0x1FF when filter ID is 0x100.
 
+rsource "Kconfig.can_random_traffic"
+
 module = CAN_UART_REPLAY
 module-str = can_uart_replay
 source "subsys/logging/Kconfig.template.log_config"
diff --git a/subsys/can_uart_replay/Kconfig.can_random_traffic b/subsys/can_uart_replay/Kconfig.can_random_traffic
new file mode 100644
index 0000000..8333566
--- /dev/null
+++ b/subsys/can_uart_replay/Kconfig.can_random_traffic
@@ -0,0 +1,46 @@
+# SPDX-FileCopyrightText: Copyright (c) 2026 Jilay Sandeep Pandya
+# SPDX-License-Identifier: Apache-2.0
+
+config CAN_RANDOM_TRAFFIC
+	bool "Generate random CAN traffic"
+	select ENTROPY_GENERATOR
+	help
+	  When enabled, the subsystem transmits randomly generated CAN frames
+	  at random intervals to exercise the bus.
+
+# ---------------------------------------------------------------------------
+# Periodic TX schedule (fixed slots, up to 3)
+# ---------------------------------------------------------------------------
+
+if CAN_RANDOM_TRAFFIC
+
+config CAN_RAND_TX_MSG_COUNT
+	int "Number of periodic CAN TX messages"
+	default 0
+	range 0 3
+	help
+	  How many periodic TX schedule entries are active (0 disables TX).
+	  Configure each active slot below (MSG0 … MSG2).
+
+# --- Slot 0 ---
+if CAN_RAND_TX_MSG_COUNT >= 1
+module = CAN_RAND_TX_MSG0
+module-str = "CAN TX message 0"
+rsource "Kconfig.template.can_tx_schedule"
+endif
+
+# --- Slot 1 ---
+if CAN_RAND_TX_MSG_COUNT >= 2
+module = CAN_RAND_TX_MSG1
+module-str = "CAN TX message 1"
+rsource "Kconfig.template.can_tx_schedule"
+endif
+
+# --- Slot 2 ---
+if CAN_RAND_TX_MSG_COUNT >= 3
+module = CAN_RAND_TX_MSG2
+module-str = "CAN TX message 2"
+rsource "Kconfig.template.can_tx_schedule"
+endif
+
+endif # CAN_RANDOM_TRAFFIC
diff --git a/subsys/can_uart_replay/Kconfig.template.can_tx_schedule b/subsys/can_uart_replay/Kconfig.template.can_tx_schedule
new file mode 100644
index 0000000..1d9243f
--- /dev/null
+++ b/subsys/can_uart_replay/Kconfig.template.can_tx_schedule
@@ -0,0 +1,15 @@
+# SPDX-FileCopyrightText: Copyright (c) 2026 Jilay Sandeep Pandya
+# SPDX-License-Identifier: Apache-2.0
+
+config $(module)_ID
+	hex "$(module-str) CAN ID"
+	range 0x000 0x7FF
+	help
+	  11-bit standard CAN ID for this periodic TX schedule entry.
+
+config $(module)_PERIOD_MS
+	int "$(module-str) period (ms)"
+	default 1000
+	range 1 3600000
+	help
+	  Transmission interval in milliseconds for this periodic TX schedule entry.
diff --git a/subsys/can_uart_replay/can_random_traffic.c b/subsys/can_uart_replay/can_random_traffic.c
new file mode 100644
index 0000000..74a232d
--- /dev/null
+++ b/subsys/can_uart_replay/can_random_traffic.c
@@ -0,0 +1,100 @@
+/*
+ * SPDX-FileCopyrightText: Copyright (c) 2026 Jilay Sandeep Pandya
+ * SPDX-License-Identifier: Apache-2.0
+ */
+
+/*
+ * Periodic random CAN traffic generator.
+ *
+ * Each active TX slot is driven by a k_work_delayable that reschedules itself
+ * after every transmission, so no extra thread is required.
+ */
+
+#include <zephyr/kernel.h>
+#include <zephyr/device.h>
+#include <zephyr/devicetree.h>
+#include <zephyr/drivers/can.h>
+#include <zephyr/random/random.h>
+#include <zephyr/logging/log.h>
+#include <zephyr/init.h>
+
+LOG_MODULE_REGISTER(can_random_traffic, CONFIG_CAN_UART_REPLAY_LOG_LEVEL);
+
+/* Maximum number of TX slots supported by this implementation. */
+#define CAN_RANDOM_TRAFFIC_MAX_SLOTS 3
+
+struct tx_slot {
+	struct k_work_delayable dwork;
+	const struct device *can_dev;
+	uint32_t id;
+	uint32_t period_ms;
+};
+
+static struct tx_slot tx_slots[CAN_RANDOM_TRAFFIC_MAX_SLOTS];
+
+/* Compile-time table of (id, period_ms) pairs derived from Kconfig. */
+static const struct {
+	uint32_t id;
+	uint32_t period_ms;
+} tx_cfg[CAN_RANDOM_TRAFFIC_MAX_SLOTS] = {
+	{CONFIG_CAN_RAND_TX_MSG0_ID, CONFIG_CAN_RAND_TX_MSG0_PERIOD_MS},
+#if CONFIG_CAN_RAND_TX_MSG_COUNT >= 2
+	{CONFIG_CAN_RAND_TX_MSG1_ID, CONFIG_CAN_RAND_TX_MSG1_PERIOD_MS},
+#else
+	{0, 1000},
+#endif
+#if CONFIG_CAN_RAND_TX_MSG_COUNT >= 3
+	{CONFIG_CAN_RAND_TX_MSG2_ID, CONFIG_CAN_RAND_TX_MSG2_PERIOD_MS},
+#else
+	{0, 1000},
+#endif
+};
+
+static void tx_work_handler(struct k_work *work)
+{
+	struct k_work_delayable *dwork = k_work_delayable_from_work(work);
+	struct tx_slot *slot = CONTAINER_OF(dwork, struct tx_slot, dwork);
+	struct can_frame frame = {
+		.id = slot->id,
+		.dlc = CAN_MAX_DLC,
+		.flags = 0,
+	};
+	int err;
+
+	sys_rand_get(frame.data, CAN_MAX_DLC);
+
+	err = can_send(slot->can_dev, &frame, K_MSEC(100), NULL, NULL);
+	if (err != 0) {
+		LOG_WRN("TX id=0x%03x failed (err %d)", slot->id, err);
+	} else {
+		LOG_INF("TX id=0x%03x", slot->id);
+		LOG_HEXDUMP_INF(frame.data, CAN_MAX_DLC, "data:");
+	}
+
+	/* Reschedule for the next period. */
+	k_work_reschedule(dwork, K_MSEC(slot->period_ms));
+}
+
+static int can_random_traffic_init(void)
+{
+	const struct device *can_dev = DEVICE_DT_GET(DT_CHOSEN(zephyr_canbus));
+
+	if (!device_is_ready(can_dev)) {
+		LOG_ERR("CAN device not ready");
+		return -ENODEV;
+	}
+
+	for (int i = 0; i < CONFIG_CAN_RAND_TX_MSG_COUNT; i++) {
+		tx_slots[i].can_dev = can_dev;
+		tx_slots[i].id = tx_cfg[i].id;
+		tx_slots[i].period_ms = tx_cfg[i].period_ms;
+		k_work_init_delayable(&tx_slots[i].dwork, tx_work_handler);
+		LOG_INF("Scheduling TX id=0x%03x every %u ms", tx_slots[i].id,
+			tx_slots[i].period_ms);
+		k_work_reschedule(&tx_slots[i].dwork, K_MSEC(tx_slots[i].period_ms));
+	}
+	LOG_INF("Periodic random TX enabled: %d slot(s)", CONFIG_CAN_RAND_TX_MSG_COUNT);
+	return 0;
+}
+
+SYS_INIT(can_random_traffic_init, APPLICATION, CONFIG_APPLICATION_INIT_PRIORITY);
-- 
2.39.5 (Apple Git-154)

